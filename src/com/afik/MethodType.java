package com.afik;

/**
 * Enum represent method that the user can run on the system.
 */
public enum MethodType
{
    about, addCourse, addUser, addHomework, editHomework, editCourse, setting, userExist,
    getCourses, getHomeworks, deleteCourse, deleteHomework;
}
